[[rules]]
id = "email-address"
enabled = true
entity_type = "EMAIL"
pattern = '(?P<value>[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,})'
group = "value"
flags = ["IGNORECASE"]
normalizers = ["strip", "lower"]
max_matches = 100
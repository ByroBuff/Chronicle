services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --appendonly yes

    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

    volumes:
      - redis_data:/data

  collector:
    build: .
    restart: unless-stopped
    command: python -m collector.runner

    environment:
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_DB: "0"
      RQ_QUEUE: article_processing

      FEEDS_CONFIG_PATH: /config/feeds.toml
      COLLECTOR_TICK_SECONDS: "60"

    depends_on:
      redis:
        condition: service_healthy

    volumes:
    - ./config:/config:ro

  worker:
    build: .
    restart: unless-stopped

    command:
      - sh
      - -c
      - |
        python -c "from database import setup_database; setup_database(); from workers.regex_extractor import get_regex_rules; get_regex_rules()"
        exec rq worker --url redis://redis:6379/0 article_processing

    environment:
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_DB: "0"
      RQ_QUEUE: article_processing
      DB_PATH: /data/chronicle.db
      ENTITY_RULES_PATH: /config/entity_rules.toml

    depends_on:
      redis:
        condition: service_healthy

    volumes:
      - sqlite_data:/data
      - ./config:/config:ro

  api:
    build: .
    restart: unless-stopped
    command: uvicorn api.main:app --host 0.0.0.0 --port 8080

    environment:
      DB_PATH: /data/chronicle.db
      CORS_ORIGINS: http://localhost:3000,http://localhost:5173

    ports:
      - "8080:8080"

    volumes:
      - sqlite_data:/data

    depends_on:
      - worker

volumes:
  redis_data:
  sqlite_data:
services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --appendonly yes

    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

    volumes:
      - redis_data:/data

  collector:
    build: .
    restart: unless-stopped
    command: python -m collector.runner

    environment:
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_DB: "0"
      RQ_QUEUE: article_processing

      FEEDS_CONFIG_PATH: /config/feeds.toml
      COLLECTOR_TICK_SECONDS: "60"

    depends_on:
      redis:
        condition: service_healthy

    volumes:
    - ./config:/config:ro

  worker:
    build: .
    restart: unless-stopped

    command:
      - sh
      - -c
      - |
        python -c "from database import setup_database; setup_database()"
        exec rq worker --url redis://redis:6379/0 article_processing

    environment:
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_DB: "0"
      RQ_QUEUE: article_processing
      DB_PATH: /data/chronicle.db

    depends_on:
      redis:
        condition: service_healthy

    volumes:
      - sqlite_data:/data

volumes:
  redis_data:
  sqlite_data: